# Use a unique secret outside local development.
ADMIN_API_KEY=change-me-before-deployment

# Comma-separated browser origins. Avoid '*' when credentials are enabled.
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

MLFLOW_TRACKING_URI=sqlite:///mlflow.db
MLFLOW_MODEL_NAME=driftguard-fraud
SERVING_URL=http://localhost:8000
