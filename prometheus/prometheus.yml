global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "driftguard-fastapi"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["serving:8000", "localhost:8000", "host.docker.internal:8000"]
